# Telegram Bot для автоматических опросов ДопТеп

Этот бот автоматически публикует неанонимный опрос каждую **среду в 11:00** по времени **Алматы/Астаны (UTC+5)**.

## Возможности

- Автоматическая отправка опросов по расписанию
- Неанонимные опросы (видно, кто как проголосовал)
- Команды для тестирования и получения ID чата
- Работает 24/7 на бесплатном хостинге

---

## 1️⃣ Создание бота через @BotFather

### Шаг 1: Получение токена бота

1. Открой Telegram и найди [@BotFather](https://t.me/BotFather)
2. Напиши команду `/start`
3. Создай нового бота командой `/newbot`
4. BotFather попросит тебя придумать:
   - **Имя бота** (например: `DopTep Poll Bot`)
   - **Username бота** (должен заканчиваться на `bot`, например: `doptep_poll_bot`)
5. После создания ты получишь **токен** вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. **Сохрани этот токен** — он понадобится при деплое!

### Шаг 2: Отключение режима приватности (ВАЖНО!)

Чтобы бот мог работать в группах, нужно отключить Privacy Mode:

1. В чате с @BotFather напиши `/mybots`
2. Выбери своего бота
3. Нажми **Bot Settings** → **Group Privacy**
4. Выбери **Turn off** (отключить)

---

## 2️⃣ Настройка группы

### Шаг 1: Добавь бота в группу

1. Открой свою Telegram-группу
2. Нажми на название группы → **Добавить участников**
3. Найди своего бота по username и добавь его

### Шаг 2: Сделай бота администратором

**ВАЖНО!** Без прав администратора бот не сможет отправлять опросы.

1. Открой группу → нажми на название группы
2. Нажми **Администраторы** → **Добавить администратора**
3. Найди своего бота и добавь его
4. Включи права:
   - ✅ **Отправка сообщений**
   - ✅ **Публикация сообщений** (если это канал)
   - Остальные права можно отключить

### Шаг 3: Получи ID группы

1. В группе напиши команду `/get_chat_id` (отправь боту)
2. Бот ответит с ID группы (например: `-1001234567890`)
3. **Сохрани этот ID** — он понадобится при деплое!

> **Примечание:** Если бот не отвечает, убедись что:
> - Бот добавлен в группу как администратор
> - Privacy Mode отключен в @BotFather

---

## 3️⃣ Тестирование локально (опционально)

Если хочешь проверить бота на своем компьютере перед деплоем:

```bash
# Установи Python 3.11+ (если нет)

# Создай виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установи зависимости
pip install -r requirements.txt

# Установи переменные окружения
export BOT_TOKEN="твой_токен_от_BotFather"
export CHAT_ID="-1001234567890"

# Запусти бота
python bot.py
```

**Команды для тестирования:**
- `/start` — приветствие и инфо о боте
- `/get_chat_id` — получить ID текущего чата
- `/test_poll` — отправить тестовый опрос прямо сейчас

---

## 4️⃣ Бесплатный деплой на Render.com

**Render.com** предоставляет бесплатный хостинг с **750 часами в месяц** (этого хватает на работу 24/7).

### Шаг 1: Загрузи код на GitHub

1. Создай новый репозиторий на [GitHub](https://github.com/new)
2. Загрузи все файлы проекта:
   - `bot.py`
   - `requirements.txt`
   - `Procfile`
   - `.env.example`
   - `README.md`

```bash
# Команды для загрузки (выполняй в папке проекта)
git init
git add .
git commit -m "Initial commit: DopTep Poll Bot"
git branch -M main
git remote add origin https://github.com/твой-username/твой-репозиторий.git
git push -u origin main
```

### Шаг 2: Регистрация на Render.com

1. Перейди на [render.com](https://render.com)
2. Нажми **Get Started** (или **Sign Up**)
3. Зарегистрируйся через **GitHub** (это удобнее)
4. Разреши Render доступ к твоему репозиторию

### Шаг 3: Создание Web Service

1. На главной странице Render нажми **New +** → **Background Worker**
2. Выбери свой репозиторий из списка
3. Заполни настройки:

**Основные настройки:**
- **Name:** `doptep-poll-bot` (или любое другое)
- **Region:** `Frankfurt (EU Central)` (ближе к Казахстану)
- **Branch:** `main`
- **Root Directory:** оставь пустым (если файлы в корне репозитория)
- **Runtime:** `Python 3`
- **Build Command:** `pip install -r requirements.txt`
- **Start Command:** `python bot.py` (или оставь пустым, если есть Procfile)

**План:**
- Выбери **Free** (бесплатный)

### Шаг 4: Добавление переменных окружения

Перед деплоем нужно добавить переменные:

1. Прокрути вниз до раздела **Environment Variables**
2. Нажми **Add Environment Variable**
3. Добавь две переменные:

| Key       | Value                          |
|-----------|--------------------------------|
| `BOT_TOKEN` | `твой_токен_от_BotFather`     |
| `CHAT_ID`   | `-1001234567890` (твой ID группы) |

### Шаг 5: Деплой

1. Нажми **Create Background Worker** (внизу страницы)
2. Render начнет деплой (это займет 2-3 минуты)
3. Дождись, пока статус изменится на **Live** (зеленый)

### Шаг 6: Проверка логов

1. Перейди на страницу твоего сервиса
2. Открой вкладку **Logs**
3. Должны увидеть:
   ```
   🚀 Бот запущен!
   ✅ Расписание настроено: опросы будут отправляться каждую среду в 11:00 (UTC+5)
   ```

---

## 5️⃣ Альтернативный деплой на Railway.app

**Railway.app** также предлагает бесплатный план с **$5 кредитов в месяц** (хватает на работу 24/7).

### Шаг 1: Регистрация

1. Перейди на [railway.app](https://railway.app)
2. Нажми **Start a New Project**
3. Войди через **GitHub**

### Шаг 2: Создание проекта

1. Нажми **New Project**
2. Выбери **Deploy from GitHub repo**
3. Выбери свой репозиторий

### Шаг 3: Настройка

1. Railway автоматически определит, что это Python-проект
2. Перейди в **Variables** (в меню слева)
3. Добавь переменные окружения:

```
BOT_TOKEN=твой_токен_от_BotFather
CHAT_ID=-1001234567890
```

### Шаг 4: Деплой

1. Railway автоматически начнет деплой
2. Проверь логи в разделе **Deployments** → **View Logs**
3. Должен увидеть сообщение о запуске бота

---

## 6️⃣ Проверка работы

После деплоя:

1. Подожди до среды 11:00 (UTC+5) для автоматического опроса
2. Или отправь команду `/test_poll` боту в группе для немедленной проверки

**Ожидаемый результат:**
- Бот отправит опрос с вопросом "🏃‍♂️ Приходишь на ДопТеп в эту среду?"
- Варианты ответа: "✅ Приду" и "❌ Не приду"
- Опрос будет **неанонимным** (видно, кто проголосовал)

---

## 7️⃣ Устранение проблем

### Бот не отвечает в группе

- ✅ Проверь, что бот добавлен как **администратор**
- ✅ Убедись, что **Privacy Mode отключен** в @BotFather
- ✅ Проверь, что `CHAT_ID` правильный (используй `/get_chat_id`)

### Бот не отправляет опросы автоматически

- ✅ Проверь логи на Render/Railway — бот должен быть запущен
- ✅ Убедись, что переменная `CHAT_ID` установлена в настройках хостинга
- ✅ Проверь, что текущий день — среда, а время — 11:00 UTC+5

### Бот упал/остановился на Render

Render на бесплатном плане может приостановить сервисы после 15 минут неактивности.
**Решение:** Используй платный план ($7/месяц) или Railway.app.

### Изменить время отправки опроса

Отредактируй строку в файле `bot.py:114`:

```python
time=time(hour=11, minute=0, second=0, tzinfo=TIMEZONE),
```

Измени `hour=11` на нужное время (например, `hour=14` для 14:00).

### Изменить день недели

Отредактируй строку в файле `bot.py:115`:

```python
days=(2,),  # 2 = среда
```

Измени цифру:
- `0` = понедельник
- `1` = вторник
- `2` = среда
- `3` = четверг
- `4` = пятница
- `5` = суббота
- `6` = воскресенье

Для нескольких дней: `days=(0, 2, 4)` (пн, ср, пт)

---

## 8️⃣ Структура проекта

```
DopTepPollBot/
├── bot.py              # Основной код бота
├── requirements.txt    # Зависимости Python
├── Procfile           # Конфигурация для хостинга
├── .env.example       # Пример переменных окружения
└── README.md          # Эта инструкция
```

---

## 9️⃣ Технические детали

- **Python:** 3.11+
- **Библиотека:** python-telegram-bot v21.0.1
- **Планировщик:** встроенный JobQueue
- **Часовой пояс:** Asia/Almaty (UTC+5)
- **Тип опроса:** неанонимный, один вариант ответа

---

## 🎉 Готово!

Теперь твой бот будет автоматически отправлять опросы каждую среду в 11:00!

**Есть вопросы?** Проверь раздел "Устранение проблем" выше или изучи логи на хостинге.

---

## 📝 Лицензия

Этот проект создан для личного использования. Можешь свободно модифицировать и адаптировать под свои нужды.
